# Acceptance overlay: use host PostgreSQL (existing business data) from containers.
services:
  api:
    environment:
      DATABASE_URL: postgresql+psycopg://biaoz:biaoz@host.docker.internal:5432/biaoz
  collection-worker:
    environment:
      DATABASE_URL: postgresql+psycopg://biaoz:biaoz@host.docker.internal:5432/biaoz
  ocr-worker:
    environment:
      DATABASE_URL: postgresql+psycopg://biaoz:biaoz@host.docker.internal:5432/biaoz
